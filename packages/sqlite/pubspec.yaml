name: sqlite
description: A Flutter implementation of the sqlite that uses sqlite db.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.3

  sqlite_api:
    path: ../sqlite_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0
