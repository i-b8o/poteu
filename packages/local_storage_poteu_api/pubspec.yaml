name: local_storage_poteu_api
description: A Flutter implementation of the PoteuApi that uses local storage.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.7.0
  rxdart: ^0.27.4
  shared_preferences: ^2.0.15
  poteu_api:
    path: ../poteu_api

dev_dependencies:
  mocktail: ^0.3.0
  very_good_analysis: ^2.4.0
