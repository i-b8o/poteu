name: sp_api
description: The interface for an API providing access to shared preferences.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:


dev_dependencies:
  very_good_analysis: ^2.4.0
